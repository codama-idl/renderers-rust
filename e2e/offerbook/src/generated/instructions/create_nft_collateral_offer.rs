//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::NftCollateralAsset;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CREATE_NFT_COLLATERAL_OFFER_DISCRIMINATOR: [u8; 8] = [48, 34, 52, 189, 81, 11, 169, 8];

/// Accounts.
#[derive(Debug)]
pub struct CreateNftCollateralOffer {
    /// The borrower creating the offer; pays for the offer account allocation.
    pub signer: solana_address::Address,
    /// Registered user profile for the signer.
    /// We want to ensure that the signer is a registered user before creating an offer.
    pub signer_user: solana_address::Address,
    /// Global protocol configuration that defines valid offer parameter ranges
    /// (duration, expiry, APY, minimum amounts) and pause state.
    pub config: solana_address::Address,
    /// Newly initialized offer account that will store all offer terms.
    pub offer: solana_address::Address,
    /// The token mint the borrower wants to receive as principal.
    pub principal_mint: solana_address::Address,
    /// Optional: the offer this one is countering. When present, must be
    /// on the opposite side (Principal), created by someone other than
    /// the signer, still fillable (Active or PartiallyFilled), and for the
    /// same principal token. The collateral asset is carried in the IX
    /// params rather than as an account, so collateral-side consistency
    /// isn't enforceable at the constraint level.
    /// When absent, the field stays at Pubkey::default().
    pub countered_offer: Option<solana_address::Address>,

    pub system_program: solana_address::Address,

    pub event_authority: solana_address::Address,

    pub program: solana_address::Address,
}

impl CreateNftCollateralOffer {
    pub fn instruction(
        &self,
        args: CreateNftCollateralOfferInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: CreateNftCollateralOfferInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(9 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.signer, true));
        accounts.push(solana_instruction::AccountMeta::new(
            self.signer_user,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.offer, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.principal_mint,
            false,
        ));
        if let Some(countered_offer) = self.countered_offer {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                countered_offer,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::OFFERBOOK_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = CreateNftCollateralOfferInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::OFFERBOOK_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateNftCollateralOfferInstructionData {
    discriminator: [u8; 8],
}

impl CreateNftCollateralOfferInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [48, 34, 52, 189, 81, 11, 169, 8],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CreateNftCollateralOfferInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateNftCollateralOfferInstructionArgs {
    pub principal_amount: u64,
    pub apy: u32,
    pub duration: u32,
    pub expiry: u32,
    pub allow_extend: bool,
    pub collateral: NftCollateralAsset,
}

impl CreateNftCollateralOfferInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `CreateNftCollateralOffer`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[writable]` signer_user
///   2. `[]` config
///   3. `[writable]` offer
///   4. `[]` principal_mint
///   5. `[optional]` countered_offer
///   6. `[optional]` system_program (default to `11111111111111111111111111111111`)
///   7. `[]` event_authority
///   8. `[]` program
#[derive(Clone, Debug, Default)]
pub struct CreateNftCollateralOfferBuilder {
    signer: Option<solana_address::Address>,
    signer_user: Option<solana_address::Address>,
    config: Option<solana_address::Address>,
    offer: Option<solana_address::Address>,
    principal_mint: Option<solana_address::Address>,
    countered_offer: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    event_authority: Option<solana_address::Address>,
    program: Option<solana_address::Address>,
    principal_amount: Option<u64>,
    apy: Option<u32>,
    duration: Option<u32>,
    expiry: Option<u32>,
    allow_extend: Option<bool>,
    collateral: Option<NftCollateralAsset>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CreateNftCollateralOfferBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The borrower creating the offer; pays for the offer account allocation.
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_address::Address) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// Registered user profile for the signer.
    /// We want to ensure that the signer is a registered user before creating an offer.
    #[inline(always)]
    pub fn signer_user(&mut self, signer_user: solana_address::Address) -> &mut Self {
        self.signer_user = Some(signer_user);
        self
    }
    /// Global protocol configuration that defines valid offer parameter ranges
    /// (duration, expiry, APY, minimum amounts) and pause state.
    #[inline(always)]
    pub fn config(&mut self, config: solana_address::Address) -> &mut Self {
        self.config = Some(config);
        self
    }
    /// Newly initialized offer account that will store all offer terms.
    #[inline(always)]
    pub fn offer(&mut self, offer: solana_address::Address) -> &mut Self {
        self.offer = Some(offer);
        self
    }
    /// The token mint the borrower wants to receive as principal.
    #[inline(always)]
    pub fn principal_mint(&mut self, principal_mint: solana_address::Address) -> &mut Self {
        self.principal_mint = Some(principal_mint);
        self
    }
    /// `[optional account]`
    /// Optional: the offer this one is countering. When present, must be
    /// on the opposite side (Principal), created by someone other than
    /// the signer, still fillable (Active or PartiallyFilled), and for the
    /// same principal token. The collateral asset is carried in the IX
    /// params rather than as an account, so collateral-side consistency
    /// isn't enforceable at the constraint level.
    /// When absent, the field stays at Pubkey::default().
    #[inline(always)]
    pub fn countered_offer(
        &mut self,
        countered_offer: Option<solana_address::Address>,
    ) -> &mut Self {
        self.countered_offer = countered_offer;
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_address::Address) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: solana_address::Address) -> &mut Self {
        self.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn principal_amount(&mut self, principal_amount: u64) -> &mut Self {
        self.principal_amount = Some(principal_amount);
        self
    }
    #[inline(always)]
    pub fn apy(&mut self, apy: u32) -> &mut Self {
        self.apy = Some(apy);
        self
    }
    #[inline(always)]
    pub fn duration(&mut self, duration: u32) -> &mut Self {
        self.duration = Some(duration);
        self
    }
    #[inline(always)]
    pub fn expiry(&mut self, expiry: u32) -> &mut Self {
        self.expiry = Some(expiry);
        self
    }
    #[inline(always)]
    pub fn allow_extend(&mut self, allow_extend: bool) -> &mut Self {
        self.allow_extend = Some(allow_extend);
        self
    }
    #[inline(always)]
    pub fn collateral(&mut self, collateral: NftCollateralAsset) -> &mut Self {
        self.collateral = Some(collateral);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CreateNftCollateralOffer {
            signer: self.signer.expect("signer is not set"),
            signer_user: self.signer_user.expect("signer_user is not set"),
            config: self.config.expect("config is not set"),
            offer: self.offer.expect("offer is not set"),
            principal_mint: self.principal_mint.expect("principal_mint is not set"),
            countered_offer: self.countered_offer,
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
            event_authority: self.event_authority.expect("event_authority is not set"),
            program: self.program.expect("program is not set"),
        };
        let args = CreateNftCollateralOfferInstructionArgs {
            principal_amount: self
                .principal_amount
                .clone()
                .expect("principal_amount is not set"),
            apy: self.apy.clone().expect("apy is not set"),
            duration: self.duration.clone().expect("duration is not set"),
            expiry: self.expiry.clone().expect("expiry is not set"),
            allow_extend: self.allow_extend.clone().expect("allow_extend is not set"),
            collateral: self.collateral.clone().expect("collateral is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `create_nft_collateral_offer` CPI accounts.
pub struct CreateNftCollateralOfferCpiAccounts<'a, 'b> {
    /// The borrower creating the offer; pays for the offer account allocation.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Registered user profile for the signer.
    /// We want to ensure that the signer is a registered user before creating an offer.
    pub signer_user: &'b solana_account_info::AccountInfo<'a>,
    /// Global protocol configuration that defines valid offer parameter ranges
    /// (duration, expiry, APY, minimum amounts) and pause state.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Newly initialized offer account that will store all offer terms.
    pub offer: &'b solana_account_info::AccountInfo<'a>,
    /// The token mint the borrower wants to receive as principal.
    pub principal_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Optional: the offer this one is countering. When present, must be
    /// on the opposite side (Principal), created by someone other than
    /// the signer, still fillable (Active or PartiallyFilled), and for the
    /// same principal token. The collateral asset is carried in the IX
    /// params rather than as an account, so collateral-side consistency
    /// isn't enforceable at the constraint level.
    /// When absent, the field stays at Pubkey::default().
    pub countered_offer: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,

    pub event_authority: &'b solana_account_info::AccountInfo<'a>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
}

/// `create_nft_collateral_offer` CPI instruction.
pub struct CreateNftCollateralOfferCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The borrower creating the offer; pays for the offer account allocation.
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Registered user profile for the signer.
    /// We want to ensure that the signer is a registered user before creating an offer.
    pub signer_user: &'b solana_account_info::AccountInfo<'a>,
    /// Global protocol configuration that defines valid offer parameter ranges
    /// (duration, expiry, APY, minimum amounts) and pause state.
    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// Newly initialized offer account that will store all offer terms.
    pub offer: &'b solana_account_info::AccountInfo<'a>,
    /// The token mint the borrower wants to receive as principal.
    pub principal_mint: &'b solana_account_info::AccountInfo<'a>,
    /// Optional: the offer this one is countering. When present, must be
    /// on the opposite side (Principal), created by someone other than
    /// the signer, still fillable (Active or PartiallyFilled), and for the
    /// same principal token. The collateral asset is carried in the IX
    /// params rather than as an account, so collateral-side consistency
    /// isn't enforceable at the constraint level.
    /// When absent, the field stays at Pubkey::default().
    pub countered_offer: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,

    pub event_authority: &'b solana_account_info::AccountInfo<'a>,

    pub program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: CreateNftCollateralOfferInstructionArgs,
}

impl<'a, 'b> CreateNftCollateralOfferCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CreateNftCollateralOfferCpiAccounts<'a, 'b>,
        args: CreateNftCollateralOfferInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            signer: accounts.signer,
            signer_user: accounts.signer_user,
            config: accounts.config,
            offer: accounts.offer,
            principal_mint: accounts.principal_mint,
            countered_offer: accounts.countered_offer,
            system_program: accounts.system_program,
            event_authority: accounts.event_authority,
            program: accounts.program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(9 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.signer.key, true));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.signer_user.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.offer.key, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.principal_mint.key,
            false,
        ));
        if let Some(countered_offer) = self.countered_offer {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *countered_offer.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::OFFERBOOK_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = CreateNftCollateralOfferInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::OFFERBOOK_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(10 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.signer.clone());
        account_infos.push(self.signer_user.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.offer.clone());
        account_infos.push(self.principal_mint.clone());
        if let Some(countered_offer) = self.countered_offer {
            account_infos.push(countered_offer.clone());
        }
        account_infos.push(self.system_program.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CreateNftCollateralOffer` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[writable]` signer_user
///   2. `[]` config
///   3. `[writable]` offer
///   4. `[]` principal_mint
///   5. `[optional]` countered_offer
///   6. `[]` system_program
///   7. `[]` event_authority
///   8. `[]` program
#[derive(Clone, Debug)]
pub struct CreateNftCollateralOfferCpiBuilder<'a, 'b> {
    instruction: Box<CreateNftCollateralOfferCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CreateNftCollateralOfferCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CreateNftCollateralOfferCpiBuilderInstruction {
            __program: program,
            signer: None,
            signer_user: None,
            config: None,
            offer: None,
            principal_mint: None,
            countered_offer: None,
            system_program: None,
            event_authority: None,
            program: None,
            principal_amount: None,
            apy: None,
            duration: None,
            expiry: None,
            allow_extend: None,
            collateral: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The borrower creating the offer; pays for the offer account allocation.
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// Registered user profile for the signer.
    /// We want to ensure that the signer is a registered user before creating an offer.
    #[inline(always)]
    pub fn signer_user(
        &mut self,
        signer_user: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.signer_user = Some(signer_user);
        self
    }
    /// Global protocol configuration that defines valid offer parameter ranges
    /// (duration, expiry, APY, minimum amounts) and pause state.
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    /// Newly initialized offer account that will store all offer terms.
    #[inline(always)]
    pub fn offer(&mut self, offer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.offer = Some(offer);
        self
    }
    /// The token mint the borrower wants to receive as principal.
    #[inline(always)]
    pub fn principal_mint(
        &mut self,
        principal_mint: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.principal_mint = Some(principal_mint);
        self
    }
    /// `[optional account]`
    /// Optional: the offer this one is countering. When present, must be
    /// on the opposite side (Principal), created by someone other than
    /// the signer, still fillable (Active or PartiallyFilled), and for the
    /// same principal token. The collateral asset is carried in the IX
    /// params rather than as an account, so collateral-side consistency
    /// isn't enforceable at the constraint level.
    /// When absent, the field stays at Pubkey::default().
    #[inline(always)]
    pub fn countered_offer(
        &mut self,
        countered_offer: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.countered_offer = countered_offer;
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    #[inline(always)]
    pub fn program(&mut self, program: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.program = Some(program);
        self
    }
    #[inline(always)]
    pub fn principal_amount(&mut self, principal_amount: u64) -> &mut Self {
        self.instruction.principal_amount = Some(principal_amount);
        self
    }
    #[inline(always)]
    pub fn apy(&mut self, apy: u32) -> &mut Self {
        self.instruction.apy = Some(apy);
        self
    }
    #[inline(always)]
    pub fn duration(&mut self, duration: u32) -> &mut Self {
        self.instruction.duration = Some(duration);
        self
    }
    #[inline(always)]
    pub fn expiry(&mut self, expiry: u32) -> &mut Self {
        self.instruction.expiry = Some(expiry);
        self
    }
    #[inline(always)]
    pub fn allow_extend(&mut self, allow_extend: bool) -> &mut Self {
        self.instruction.allow_extend = Some(allow_extend);
        self
    }
    #[inline(always)]
    pub fn collateral(&mut self, collateral: NftCollateralAsset) -> &mut Self {
        self.instruction.collateral = Some(collateral);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = CreateNftCollateralOfferInstructionArgs {
            principal_amount: self
                .instruction
                .principal_amount
                .clone()
                .expect("principal_amount is not set"),
            apy: self.instruction.apy.clone().expect("apy is not set"),
            duration: self
                .instruction
                .duration
                .clone()
                .expect("duration is not set"),
            expiry: self.instruction.expiry.clone().expect("expiry is not set"),
            allow_extend: self
                .instruction
                .allow_extend
                .clone()
                .expect("allow_extend is not set"),
            collateral: self
                .instruction
                .collateral
                .clone()
                .expect("collateral is not set"),
        };
        let instruction = CreateNftCollateralOfferCpi {
            __program: self.instruction.__program,

            signer: self.instruction.signer.expect("signer is not set"),

            signer_user: self
                .instruction
                .signer_user
                .expect("signer_user is not set"),

            config: self.instruction.config.expect("config is not set"),

            offer: self.instruction.offer.expect("offer is not set"),

            principal_mint: self
                .instruction
                .principal_mint
                .expect("principal_mint is not set"),

            countered_offer: self.instruction.countered_offer,

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            program: self.instruction.program.expect("program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CreateNftCollateralOfferCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    signer_user: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    offer: Option<&'b solana_account_info::AccountInfo<'a>>,
    principal_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    countered_offer: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    program: Option<&'b solana_account_info::AccountInfo<'a>>,
    principal_amount: Option<u64>,
    apy: Option<u32>,
    duration: Option<u32>,
    expiry: Option<u32>,
    allow_extend: Option<bool>,
    collateral: Option<NftCollateralAsset>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
