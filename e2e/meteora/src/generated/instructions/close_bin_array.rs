//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLOSE_BIN_ARRAY_DISCRIMINATOR: [u8; 8] = [68, 174, 88, 80, 181, 204, 19, 224];

/// Accounts.
#[derive(Debug)]
pub struct CloseBinArray {
    pub lb_pair: solana_address::Address,

    pub bin_array: solana_address::Address,

    pub rent_receiver: solana_address::Address,

    pub signer: solana_address::Address,
}

impl CloseBinArray {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.lb_pair,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.bin_array, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.rent_receiver,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = CloseBinArrayInstructionData::new().try_to_vec().unwrap();

        solana_instruction::Instruction {
            program_id: crate::LB_CLMM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CloseBinArrayInstructionData {
    discriminator: [u8; 8],
}

impl CloseBinArrayInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [68, 174, 88, 80, 181, 204, 19, 224],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CloseBinArrayInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `CloseBinArray`.
///
/// ### Accounts:
///
///   0. `[]` lb_pair
///   1. `[writable]` bin_array
///   2. `[writable, signer]` rent_receiver
///   3. `[signer]` signer
#[derive(Clone, Debug, Default)]
pub struct CloseBinArrayBuilder {
    lb_pair: Option<solana_address::Address>,
    bin_array: Option<solana_address::Address>,
    rent_receiver: Option<solana_address::Address>,
    signer: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CloseBinArrayBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn lb_pair(&mut self, lb_pair: solana_address::Address) -> &mut Self {
        self.lb_pair = Some(lb_pair);
        self
    }
    #[inline(always)]
    pub fn bin_array(&mut self, bin_array: solana_address::Address) -> &mut Self {
        self.bin_array = Some(bin_array);
        self
    }
    #[inline(always)]
    pub fn rent_receiver(&mut self, rent_receiver: solana_address::Address) -> &mut Self {
        self.rent_receiver = Some(rent_receiver);
        self
    }
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_address::Address) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CloseBinArray {
            lb_pair: self.lb_pair.expect("lb_pair is not set"),
            bin_array: self.bin_array.expect("bin_array is not set"),
            rent_receiver: self.rent_receiver.expect("rent_receiver is not set"),
            signer: self.signer.expect("signer is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `close_bin_array` CPI accounts.
pub struct CloseBinArrayCpiAccounts<'a, 'b> {
    pub lb_pair: &'b solana_account_info::AccountInfo<'a>,

    pub bin_array: &'b solana_account_info::AccountInfo<'a>,

    pub rent_receiver: &'b solana_account_info::AccountInfo<'a>,

    pub signer: &'b solana_account_info::AccountInfo<'a>,
}

/// `close_bin_array` CPI instruction.
pub struct CloseBinArrayCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub lb_pair: &'b solana_account_info::AccountInfo<'a>,

    pub bin_array: &'b solana_account_info::AccountInfo<'a>,

    pub rent_receiver: &'b solana_account_info::AccountInfo<'a>,

    pub signer: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> CloseBinArrayCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CloseBinArrayCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            lb_pair: accounts.lb_pair,
            bin_array: accounts.bin_array,
            rent_receiver: accounts.rent_receiver,
            signer: accounts.signer,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.lb_pair.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.bin_array.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.rent_receiver.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let data = CloseBinArrayInstructionData::new().try_to_vec().unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::LB_CLMM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.lb_pair.clone());
        account_infos.push(self.bin_array.clone());
        account_infos.push(self.rent_receiver.clone());
        account_infos.push(self.signer.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CloseBinArray` via CPI.
///
/// ### Accounts:
///
///   0. `[]` lb_pair
///   1. `[writable]` bin_array
///   2. `[writable, signer]` rent_receiver
///   3. `[signer]` signer
#[derive(Clone, Debug)]
pub struct CloseBinArrayCpiBuilder<'a, 'b> {
    instruction: Box<CloseBinArrayCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CloseBinArrayCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CloseBinArrayCpiBuilderInstruction {
            __program: program,
            lb_pair: None,
            bin_array: None,
            rent_receiver: None,
            signer: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn lb_pair(&mut self, lb_pair: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.lb_pair = Some(lb_pair);
        self
    }
    #[inline(always)]
    pub fn bin_array(&mut self, bin_array: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.bin_array = Some(bin_array);
        self
    }
    #[inline(always)]
    pub fn rent_receiver(
        &mut self,
        rent_receiver: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.rent_receiver = Some(rent_receiver);
        self
    }
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = CloseBinArrayCpi {
            __program: self.instruction.__program,

            lb_pair: self.instruction.lb_pair.expect("lb_pair is not set"),

            bin_array: self.instruction.bin_array.expect("bin_array is not set"),

            rent_receiver: self
                .instruction
                .rent_receiver
                .expect("rent_receiver is not set"),

            signer: self.instruction.signer.expect("signer is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CloseBinArrayCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    lb_pair: Option<&'b solana_account_info::AccountInfo<'a>>,
    bin_array: Option<&'b solana_account_info::AccountInfo<'a>>,
    rent_receiver: Option<&'b solana_account_info::AccountInfo<'a>>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
